<Page
    x:Class="XBMCRemoteRT.Pages.InputPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XBMCRemoteRT.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <!--<FontFamily x:Key="ContentControlThemeFontFamily">Segoe WP</FontFamily>
		<SolidColorBrush x:Key="AppBarItemBackgroundThemeBrush" Color="Transparent"/>-->
        <Style x:Key="BigRoundButton" TargetType="AppBarButton">
            <Setter Property="Foreground" Value="{ThemeResource AppBarItemForegroundThemeBrush}"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="AppBarButton">
                        <StackPanel x:Name="LayoutRoot" Background="Transparent" Width="80">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="ApplicationViewStates">
                                    <VisualState x:Name="FullSize"/>
                                    <VisualState x:Name="Compact">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility" Storyboard.TargetName="TextLabel">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="Collapsed"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Width" Storyboard.TargetName="LayoutRoot">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="48"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Stroke" Storyboard.TargetName="Ellipse">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AppBarItemForegroundThemeBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Fill" Storyboard.TargetName="Ellipse">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AppBarItemForegroundThemeBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground" Storyboard.TargetName="Content">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AppBarItemPressedForegroundThemeBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Stroke" Storyboard.TargetName="Ellipse">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AppBarItemDisabledForegroundThemeBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground" Storyboard.TargetName="Content">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AppBarItemDisabledForegroundThemeBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Foreground" Storyboard.TargetName="TextLabel">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AppBarItemDisabledForegroundThemeBrush}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Grid HorizontalAlignment="Center" Height="80" Margin="0,12,0,4" Width="80">
                                <Ellipse x:Name="Ellipse" Fill="{ThemeResource AppBarItemBackgroundThemeBrush}" Height="80" Stroke="{ThemeResource AppBarItemForegroundThemeBrush}" StrokeThickness="1.5" UseLayoutRounding="False" Width="80"/>
                                <Grid x:Name="ContentRoot" Background="Transparent">
                                    <ContentPresenter x:Name="Content" AutomationProperties.AccessibilityView="Raw" Content="{TemplateBinding Icon}" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                                </Grid>
                            </Grid>
                            <TextBlock x:Name="TextLabel" Foreground="{ThemeResource AppBarItemForegroundThemeBrush}" FontSize="9.5" FontFamily="{TemplateBinding FontFamily}" Margin="0,0,0,10.5" TextAlignment="Center" TextWrapping="Wrap" Text="{TemplateBinding Label}" Width="70.5"/>
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock Text="{StaticResource AppName}" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
            <TextBlock Text="remote" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <!--TODO: Content should be placed within the following grid-->
        <Grid Grid.Row="1" x:Name="ContentRoot" Margin="10,0,10,0">
            <Slider Value="10" Minimum="0" Maximum="100" StepFrequency="10" x:Name="VolumeSlider" ValueChanged="VolumeSlider_ValueChanged" Loaded="VolumeSlider_Loaded" Margin="0,200,0,0" VerticalAlignment="Top"/>
            <Grid VerticalAlignment="Bottom" Margin="0,0,0,10" Height="180">
                <Grid.RowDefinitions>
                    <RowDefinition Height="120"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" >
                    <AppBarButton Style="{StaticResource LargeRoundButton}" VerticalAlignment="Center">
                        <AppBarButton.Icon>
                            <BitmapIcon UriSource="ms-appx:///Assets/Icons/previous.png" Foreground="{StaticResource PhoneForegroundBrush}" Margin="14"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarButton Style="{StaticResource LargeRoundButton}" VerticalAlignment="Center">
                        <AppBarButton.Icon>
                            <BitmapIcon UriSource="ms-appx:///Assets/Icons/rewind.png" Foreground="{StaticResource PhoneForegroundBrush}" Margin="14"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                </StackPanel>
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
                    <AppBarButton Style="{StaticResource ExtraLargeRoundButton}">
                        <AppBarButton.Icon>
                            <BitmapIcon UriSource="ms-appx:///Assets/Icons/play.png" Foreground="{StaticResource PhoneForegroundBrush}" Margin="5,0,0,0"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarButton Style="{StaticResource ExtraLargeRoundButton}" Margin="-7,0,0,0">
                        <AppBarButton.Icon>
                            <BitmapIcon UriSource="ms-appx:///Assets/Icons/stop.png" Foreground="{StaticResource PhoneForegroundBrush}" Margin="28"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                </StackPanel>
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
                    <AppBarButton Style="{StaticResource LargeRoundButton}" VerticalAlignment="Center">
                        <AppBarButton.Icon>
                            <BitmapIcon UriSource="ms-appx:///Assets/Icons/fastforward.png" Foreground="{StaticResource PhoneForegroundBrush}" Margin="14"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarButton Style="{StaticResource LargeRoundButton}" VerticalAlignment="Center" >
                        <AppBarButton.Icon>
                            <BitmapIcon UriSource="ms-appx:///Assets/Icons/next.png" Foreground="{StaticResource PhoneForegroundBrush}" Margin="14"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                </StackPanel>
                <Grid Grid.Row="1">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,-20,0,0">
                        <AppBarButton Style="{StaticResource LargeRoundButton}" VerticalAlignment="Center">
                            <AppBarButton.Icon>
                                <BitmapIcon UriSource="ms-appx:///Assets/Icons/return.png" Foreground="{StaticResource PhoneForegroundBrush}" Margin="14"/>
                            </AppBarButton.Icon>
                        </AppBarButton>
                        <AppBarButton Style="{StaticResource LargeRoundButton}" VerticalAlignment="Center">
                            <AppBarButton.Icon>
                                <BitmapIcon UriSource="ms-appx:///Assets/Icons/home.png" Foreground="{StaticResource PhoneForegroundBrush}" Margin="14"/>
                            </AppBarButton.Icon>
                        </AppBarButton>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Page>
